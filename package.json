{
  "name": "codex-web",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "check": "node scripts/check-syntax.js",
    "test": "node --test",
    "validate": "npm run check && npm test"
  }
}
