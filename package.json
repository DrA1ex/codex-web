{
  "name": "codex-web",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "check": "node scripts/check-syntax.js",
    "test": "node --test test/*.test.js",
    "validate": "npm run check && npm test && npm run e2e",
    "e2e": "playwright test",
    "e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "playwright": "1.61.1"
  }
}
